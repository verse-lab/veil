import ProofWidgets.Component.RefreshComponent

/-!
Veil used to vendor `RefreshComponent`. ProofWidgets now provides the component
directly, so this module remains as a compatibility import for Veil modules.
-/
